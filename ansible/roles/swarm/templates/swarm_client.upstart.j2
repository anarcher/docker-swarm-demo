description "Docker Swarm Agent(Join)"
author "anarcher"
console log
start on filesystem
stop on runlevel [!2345]
respawn
script
/usr/local/bin/swarm join \
    --addr {{ docker_host }} \
    {{ swarm_discovery_string }}
end script


